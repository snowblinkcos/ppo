# ============================================================
#  训练参数配置  —  直接修改这里的数值即可，不需要命令行
# ============================================================

# 训练轮数（越多越慢，但结果更好）
N_ITER = 2000

# 每轮采样的相位掩膜数量（论文：32；越大越稳定但越慢）
M = 256

# PPO 内循环更新次数（论文：4）
K = 4

# 策略探索标准差（仿真推荐 0.3；论文物理实验：0.04）
SIGMA = 0.3

# Adam 学习率
LR = 0.3

# PPO 截断参数 ε
EPSILON = 0.02

# 目标 ROI 编号（0~9，对应 3-4-3 排列中的第几个方块）
# 编号顺序：第一行 0,1,2 | 第二行 3,4,5,6 | 第三行 7,8,9
TARGET_STRIP = 4

# ── 物理噪声参数 ──────────────────────────────────────────
# SLM 量化位数（真实 SLM 通常 8-bit；0 = 关闭量化）
N_BITS = 8

# SLM 相位噪声标准差 [rad]（模拟 SLM 漂移/振动；0 = 关闭）
SLM_NOISE = 0.25

# 相机散粒噪声峰值光子数（越小噪声越大；0 = 关闭）
PEAK_PHOTONS = 50

# 关闭所有噪声（纯仿真，ER 会到 85%）
NO_NOISE = False

# ── 其他 ─────────────────────────────────────────────────
# 每隔多少轮打印一次 ER
EVAL_EVERY = 20

# 结果保存目录
SAVE_DIR = "./results"

# 随机种子
SEED = 42
